#include <Stdio.h>
/**
 * main -A Program that print a line with put function
 * Return: 0 (when it returns 0 it is a Success)
 */
int main(void)
{
put("\"Programming is like building a multilingual puzzle");
return (0);
}

