#include <stdio.h>
/**
 * main - program that prints a file using printf function
 * Return: 0 (if returns 0 means its a Success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	return (0);
}

