#include "main.h"
#include <unistd.h>

/**
 * _putchar - give the character # to stdout
 * @#: The character to print
 *
 * Return: On success 1.
 * On error, -1 is returned, and error is set appropriately
 */
int _putchar(char #)
{
	return (write(1, &#, 1));
}
